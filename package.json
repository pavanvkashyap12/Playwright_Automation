{
  "name": "plawrightautomation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "ui": "npx playwright test --grep @UI",
    "api": "npx playwright test --grep @API",
    "e2e": "npx playwright test --grep @E2E",
    "allure": "npx allure generate ./allure-results --clean && npx allure open ./allure-report",
    "test": "npx playwright test",
    "firefox": "npx playwright test --config=playwright.config1.js --project=firefox",
    "chromium": "npx playwright test --config=playwright.config1.js --project=chromium"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cucumber/cucumber": "^12.7.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "allure-playwright": "^3.5.0",
    "exceljs": "^4.4.0"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
